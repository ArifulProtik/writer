import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Write a story | Writer',
  description: 'Write a story with Writer',
};
const Write = () => {
  return (
    <div>
      Writer
    </div>
  )
}

export default Write
